#include<stdio.h>
#include<stdlib.h>
#include<string.h>
void push(int);
void main()
{
    int i,n,a[50];
    printf("\n\nenter the size of stack: ");
    scanf("%d",&n);
    for(i=1;i<=n;i++)
    {
        printf("enter the %d value into stack: ");
        scanf
    }

}